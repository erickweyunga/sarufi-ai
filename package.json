{
  "name": "conversation-framework",
  "version": "0.1.0",
  "description": "A TypeScript framework for building conversational applications",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir=dist",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^2.0.0-beta.8",
    "@ai-sdk/google": "^2.0.0-beta.14",
    "@ai-sdk/openai": "^1.3.23",
    "ai": "^5.0.0-beta.26",
    "zod": "^4.0.10"
  }
}